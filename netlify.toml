[build]
  command = "prisma generate && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# تجاوز فحص الثغرات الأمنية مؤقتاً
[build.environment]
  NODE_VERSION = "20"
